describe("Header Component", () => {
  beforeEach(() => {
    console.log("did it work");
  });

  it("should successfully load", () => {
    expect(2 + 2).toEqual(4);
  });
  it("should fail here", () => {
    expect(2 + 4).toEqual(4545);
  });
});
